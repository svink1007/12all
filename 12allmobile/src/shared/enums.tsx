export enum Gender {
  Man = "man",
  Woman = "woman",
  Other = "other",
}

export enum InAppCodeType {
  PaidSubscription = "paid subscription",
}

export enum ShareStreamOption {
  Stream,
  File,
  Torrent,
  Channel,
  Camera,
  Screen,
  Card,
}
